spring:
  application:
    name: quiz-service
  cloud:
    config:
      enabled: false

security:
  oauth2:
    resource:
      id: test_resource_id

eureka:
  client:
    enabled: false

some:
  value: 'Dupa'
  other: 'Pupa'