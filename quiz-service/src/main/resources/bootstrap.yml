spring:
  application:
    name: quiz-service
  cloud:
    config:
      uri: http://localhost:8888
      fail-fast: true
      username: config
      password: password #${CONFIG_SERVICE_PASSWORD}

encrypt:
  key: supersecretpassword