spring:
  application:
    name: fulfillment-service
  cloud:
    config:
      uri: http://config-server:8888
      fail-fast: true
      username: config
      password: password #${CONFIG_SERVICE_PASSWORD}
